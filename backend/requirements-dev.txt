# Pronokif backend — development & QA dependencies.
# Install on top of requirements.txt:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt

# Runtime deps used by middleware/security.py (slowapi rate limiter)
# now live in backend/requirements.txt so production gets actual rate
# limiting instead of the silent no-op fallback. Keep this file for
# tooling deps only.

# -- Tests --
pytest-asyncio==0.24.0    # async test cases for FastAPI
pytest-cov==5.0.0         # coverage reporting (target >70% in S1-7)
httpx==0.28.1             # match runtime pin used by FastAPI tests
respx==0.21.1             # mock HTTPX calls (OpenF1, Jolpica)

# -- Linting & formatting --
ruff==0.15.13             # fast linter, replaces flake8

# -- Type checking --
types-requests==2.32.0.20241016
types-python-dateutil==2.9.0.20241003

# -- Security scanning --
# gitleaks is installed system-wide via Homebrew (brew install gitleaks).
pip-audit==2.7.3          # CVE scan over installed wheels
bandit[toml]==1.7.10      # static security checks on Python code
